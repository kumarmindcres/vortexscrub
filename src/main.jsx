import React from 'react';
import ReactDOM from 'react-dom/client';
import VortexScrubPage from './VortexScrubPage';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <VortexScrubPage />
  </React.StrictMode>
);
